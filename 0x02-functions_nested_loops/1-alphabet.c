#include "main.h"

/**
*print_alphabet - function that print alphabet
*it prints in lowercase
*Return: no return
*/
void print_alphabet(void)
{
int c = 'a';
for (c = 'a'; c <= 'z'; c++)
_putchar(c);
_putchar('\n');

return;
}
