#include <stdio.h>

/**
 * main - Entry point
 *
 * Description:  the program prints the numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int c = 0;
while (c < 10)
{
printf("%d", c);
c++;
}
printf("\n");

return (0);
}
